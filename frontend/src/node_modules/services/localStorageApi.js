export default class localStorageApi {
    static getItem(value = 'token') {
        return localStorage[value];
    }
    static setItem(prop, value) {
        localStorage[prop] = value;
    }
    static removeItem(prop) {
        localStorage.removeItem(prop);
    }
    static clear() {
        localStorage.clear();
    }
}
