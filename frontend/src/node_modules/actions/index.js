import {CHANGE_CURRENT_CHAT} from "constants/action-types";

export const REGISTER = 'REGISTER';
export const LOGIN = 'LOGIN';

export const isLoading = type => ({type: `${type}_LOAD`});
export const isSuccess = type => ({type: `${type}_SUCCESS`});
export const isFailure = type => ({type: `${type}_FAILURE`});
export const changeCurrentChat = chat_id => ({type: CHANGE_CURRENT_CHAT, payload: chat_id});
export const changeChatLastMessage = (chat_id, last_message) => ({
    type: CHANGE_CHAT_LAST_MESSAGE,
    payload: {chat_id, last_message}
});
export const