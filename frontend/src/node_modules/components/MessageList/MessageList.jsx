import React from 'react';
import { MessageList } from 'react-chat-elements'

export default function Chats({chatList, renderChatList, moveToDirectChat}) {
    return (
        <MessageList
            className='message-list'
            lockable={true}
            toBottomHeight={'100%'}
            dataSource={[]} 
        />
    )
}
