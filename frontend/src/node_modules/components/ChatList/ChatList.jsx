import React from 'react';
import { ChatList } from 'react-chat-elements';

export default function Chats({chatList, renderChatList, moveToDirectChat}) {
    return (
        <ChatList
            className='chat-list'
            dataSource={renderChatList(chatList)}
            onClick={moveToDirectChat}
        />
    )
}
