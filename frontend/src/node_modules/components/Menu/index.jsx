import React from 'react'
import LoggedInMenu from './LoggedIn';
import LoggedOutMenu from './LoggedOut';

export default function Menu (props) {
    console.log(props)
    return props.isLoggedIn ? <LoggedInMenu {...props}/> : <LoggedOutMenu {...props}/>;
}