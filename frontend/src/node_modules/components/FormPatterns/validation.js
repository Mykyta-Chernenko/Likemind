export const validateRegister = values => {
    const errors = {};
    if (!values.email) {
        errors.email = 'This field needs to be filled.';
    }
    if (!values.username) {
        errors.username = 'This field needs to be filled.';
    }
    if (!values.password) {
        errors.password = 'This field needs to be filled.';
    }
    if (!values.password_confirmation) {
        errors.password_confirmation = 'This field needs to be filled.';
    } else if (values.password !== values.password_confirmation) {
        errors.password_confirmation = 'Confirm pass is not the same as pass.';
    }
    return errors;
};

export const validateLogin = values => {
    const errors = {};
    if (!values.email) {
        errors.email = 'This field needs to be filled.';
    }
    if (!values.password) {
        errors.password = 'This field needs to be filled.';
    }
    return errors;
};
