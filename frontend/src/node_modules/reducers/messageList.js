import { GET_MESSAGE_LIST } from '../constants';

export default (state = {
    messageList: []
}, action) => {
    switch (action.type) {
        case GET_MESSAGE_LIST: 
            return { messageList: [...action.payload] }
        default:
            return state;
    }
};