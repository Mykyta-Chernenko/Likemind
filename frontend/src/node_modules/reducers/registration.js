import { user } from '../constants';

export default (state = {}, action) => {
    switch (action.type) {
        case user.REGISTER_FAILURE:
            return {};
        case user.REGISTER_SUCCESS:
            return {};
        case user.REGISTER_LOAD:
            return { registering: true };
        default:
            return state;
    }
}