import { userConstants } from '../constants';

export default (state = {}, action) => {
    switch (action.type) {
        case userConstants.REGISTER_FAILURE:
            return {};
        case userConstants.REGISTER_SUCCESS:
            return {};
        case userConstants.REGISTER_LOAD:
            return { registering: true };
        default:
            return state;
    }
}