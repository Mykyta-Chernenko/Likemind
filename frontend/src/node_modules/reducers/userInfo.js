import { GET_USER_INFO } from '../constants';

export default (state = {}, action) => {
    switch (action.type) {
        case GET_USER_INFO:
            return {
                ...state,
                ...action.payload
            };
        default:
            return state;
    }
}
